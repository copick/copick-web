# Do not enable legacy-peer-deps here.
# It causes `npm install` to omit peer dependencies from package-lock.json,
# which makes `npm ci` fail in clean environments (e.g. the Docker build,
# which does not copy this .npmrc). Keep the lock file peer-complete.
