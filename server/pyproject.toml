[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "copick-web"
version = "0.1.2"
description = "Web visualization for copick datasets"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn>=0.25.0",
    "pydantic>=2.5.0",
    "pydantic-settings>=2.0.0",
    "copick>=2.0.0a1,<2.1",
    # The proxy constructs native byte-range requests and response buffers
    # against the public asynchronous Store API.
    "zarr>=3.1.6,<4",
    "python-dotenv>=1.0.0",
    "click>=8.1.8",
]

[project.optional-dependencies]
dev = [
    "black>=25.1.0",
    "ruff>=0.12.0",
    "pytest>=8.0.0",
    "httpx>=0.27.0",
    "pre-commit>=4.0.0",
]

[project.scripts]
copick-web = "copick_web.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/copick_web"]

[tool.black]
line-length = 120
target-version = ['py311']

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "C4"]
ignore = ["E501"]
